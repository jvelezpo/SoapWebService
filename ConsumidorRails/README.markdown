#Config

### nokogiri requirements
    `sudo apt-get install libxslt-dev libxml2-dev`
    `sudo gem install nokogiri`

###Bundle
    `bundle install`

###db
    `rake db:migrate`


#run
    `rails s`
